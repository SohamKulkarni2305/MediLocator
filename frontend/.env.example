# Set this to the deployed backend origin for production, for example:
# VITE_API_BASE_URL=https://your-backend.example.com/api
VITE_API_BASE_URL=/api
# Used when VITE_API_BASE_URL is left as the local `/api` proxy path.
VITE_BACKEND_URL=http://localhost:3001
